@using KeaPulss.Models.ViewModels;
@using KeaPulss.Models;


@model HeaderViewModel

<header>
    <div id="headerContainer">
        <div id="headerLogo">
            <div class="header-left">
                @if (Model.Logo != null)
                {
                    <img src="@Model.Logo.Url()" alt="Logo" class="logo">
                }
                else
                {
                    @* Fallback footer logo *@
                    <svg class="max-w-[116px] max-h-[58px] ">
                        <use class="" xlink:href="/assets/spritemap.svg#logo-primary" />
                    </svg>
                }
                <div class="tab" >
                    <a href="@Model.NewsUrl" class="login-btn"> News</a>
                </div>
                <div class="tab">
                    <a href="@Model.EventUrl" class="login-btn">Events</a>
                </div>
                <div class="tab">
                    <a href="@Model.StudielivUrl" class="login-btn">Studieliv</a>
                </div>
                <div class="header-right">
                    <a href="@Model.LoginUrl" class="login-btn">Log In</a>
                </div>
                <div class="header-right">
                    <a href="@Model.FredagsbarUrl" class="login-btn">Fredagsbar</a>
                </div>
                <div class="header-right">
                    <a href="@Model.ApplicationUrl" class="login-btn">applications</a>
                </div>
                <div class="header-right">
                    <a href="@Model.ParisAftaleUrlUrl" class="login-btn">Paris Aftalen</a>
                </div>

                <div class="header-right">
                    <a href="@Model.FrontPageUrl" class="login-btn">FrontPage</a>
                </div>
            </div>
           
        </div>
       
    </div>
</header>





